# Agent framework
langgraph
langchain
langchain-core

# Models (model-agnostic core — both installed; pick at runtime via env)
langchain-ollama       # local open-weight models via Ollama
langchain-anthropic    # Claude API

# Backend (added at M2)
fastapi
uvicorn[standard]

# Utilities
python-frontmatter     # parse note frontmatter
pydantic
python-dotenv

# Retrieval stage 2 (M3): local embeddings + NumPy cosine. No external service,
# so the eval harness stays hermetic for CI (spec §11).
sentence-transformers   # all-MiniLM-L6-v2, runs in-process, $0/call
numpy
