# Electron shell (M4)

Desktop UI for PIA — your aesthetic. Talks to the FastAPI backend over
localhost. Not started until the AI works and is evaluated (M1–M3).

Planned: Electron + React + Vite. Surfaces: chat, note browser, settings,
audit-log viewer. Tray/background mode is a stretch goal (M5).

Bring aesthetic references when starting M4.
